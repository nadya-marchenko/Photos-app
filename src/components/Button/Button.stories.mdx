import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Button from './Button';

<Meta
  title="Components/Button"
  component={Button}
  argTypes={{
    variant: {
      color: ['primary', 'default'],
      type: ['submit']
    }
  }}
/>

# Button 

### USAGE

Buttons are used to initialize an actions.

### EXAMPLES

export const Template = args => <Button {...args} />

<Canvas>
  <Story name='Primary' args={{ color: 'primary', children: 'Primary' }}>
    {Template.bind({})}
  </Story>
  <Story name='Default' args={{ color: 'default', children: 'Default' }}>
    {Template.bind({})}
  </Story>
  <Story name='Button with link' args={{ color: 'primary', link: '/profile', children: 'Button with link' }}>
    {Template.bind({})}
  </Story>
</Canvas>